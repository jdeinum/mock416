package mockSQL;

public class SQL {
    public static String sanitizeString(String string) {
        // mock sanitization
        return string;
    }

    public void executeQuery(String query) {
    }
}
